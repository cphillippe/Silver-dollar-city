/** Hard Night Watch Lock In chrome (player-facing). */
export function defendToolLocked(label: string): string {
  return `${label} is locked. Lock in a matching line to deploy this tool.`
}
export const DEFEND_LOCK_NAME = 'Lock in a line to name this tool.'
export const DEFEND_LOCK_SHORT = 'Lock in a matching line'
